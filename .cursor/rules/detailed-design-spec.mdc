---
description: 详细设计文档规范（写 docs/detailed-design 下任何文档时强制应用）
globs:
  - "docs/detailed-design/**"
alwaysApply: false
---

# 详细设计文档规则

权威规范：`docs/详细设计规范V1.md`；模板：`docs/detailed-design/_TEMPLATE_详细设计.md`。生成详细设计请优先使用 skill `detailed-design`。

## 结构

- 固定 10 章（概述与范围/领域模型/接口设计/数据模型/核心流程/错误码/一致性/缓存与性能/安全合规/可观测与测试），不得增删；空章写"不适用+原因"。
- 第 1 章必须回链功能点编号（D*.F*），并显式列"非目标"。
- 文件命名：`docs/detailed-design/<domain>/<service>_详细设计V<n>.md`。

## 硬约束

- 契约先行：gRPC/事件契约变更必须落到 `contracts/proto/`，文档只引用不复制定义。
- 金额一律字符串高精度（`"12000.00"` + currency）；DDL 用 `numeric(18,2)`；禁止 float/JSON number 表示金额。
- 每个用例时序图必须含 alt/else 异常路径（校验失败、下游超时、部分失败补偿）；状态机单向流转。
- 错误码格式 `<域前缀>-<4|5><三位>`（前缀目录见规范 §6.2），每码给四元组：触发条件/合规文案/系统动作/是否留痕。
- 一致性四问必须有答案：幂等键？事务边界（事务内禁远程调用）？Outbox 事件？Saga 补偿表？
- PII 字段一律 `_enc` 密文列 + 可查询时 `_hash` 列；SEC-01~09 清单逐项声明。
- Redis key 按 `pension:{domain}:{entity}:{id}`，显式 TTL。
- 指标命名 `pension_<domain>_<subject>_<unit>`。
- 所有 mermaid 提交前须可渲染（避免节点/边标签中使用括号等易破坏解析的字符）。
- 验收口径必须可测（分位数/样本量/统计窗口），禁止"100%、0 差错"类表述（资金一致性除外）。
- 新领域事件必须同步登记到规范附录 A 与 `EventTypes`。

## 职责边界（不得违反）

- 适当性：D3 只"判定"，D5 才"阻断"。
- 交易风控（D5.6）≠ AML（S2.4），不得混设计。
- 标签计算与查询分离；触达频控与去重分离；内容创作/审核/推荐三权分立（见职责分解 V1 §十）。
